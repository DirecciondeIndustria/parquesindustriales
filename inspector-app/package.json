{
  "name": "sigpip-inspecciones",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step needed - HTML is the app'",
    "sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
